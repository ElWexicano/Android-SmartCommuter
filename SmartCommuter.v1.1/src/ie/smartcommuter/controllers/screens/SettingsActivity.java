package ie.smartcommuter.controllers.screens;

import ie.smartcommuter.R;
import ie.smartcommuter.controllers.SmartActivity;
import android.os.Bundle;

/**
 * This is a class is used for the Settings
 * Screen of the Application.
 * @author Shane Bryan Doyle
 */
public class SettingsActivity extends SmartActivity {
    @Override
    public void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.screen_settings);
        
        // TODO: Create the settings for the app. (Report Bug feature, Refresh Realtime Seconds, Notifications)
        
    }
}
